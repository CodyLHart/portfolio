# Shared packages

Use this folder only when multiple apps genuinely need the same code. Keeping
shared code small helps each portfolio project remain independent and makes it
clear which skills or frameworks each app is meant to demonstrate.

Examples that can make sense here later:

- design tokens
- small TypeScript utilities
- shared data schemas
- reusable test helpers
